---
layout: default
title: 137회-4교시-3번 쿠버네티스
parent: 📝 기출문제
grand_parent: DS (Digital Service)
nav_order: 4
permalink: /docs/ds/exam/137-4-3-kubernetes
---

# 쿠버네티스 (Kubernetes)
{: .fs-8 }

137회 정보관리기술사 4교시 3번
{: .label .label-blue }

디지털서비스 / 난이도: 중
{: .label .label-green }

---

## 📚 목차

1. [고딩 수준 설명](#-고딩-수준-설명)
2. [기술사 수준 설명](#-기술사-수준-설명)

---

# 🎓 고딩 수준 설명
{: .fs-7 }

> 고등학생도 이해할 수 있게 설명합니다

---

## 🔑 핵심 키워드 3개

| 키워드 | 설명 |
|:------|:-----|
| **컨테이너 오케스트레이션** | 수백 개의 컨테이너를 자동으로 관리하는 지휘자 |
| **자동 스케일링** | 트래픽이 많으면 서버 자동 증가, 적으면 자동 감소 |
| **자가 치유** | 서버가 죽으면 자동으로 새 서버 띄워줌 |

---

## 📖 등장배경

```
🐳 Docker로 컨테이너 만들기 쉬워짐

💡 문제: "컨테이너가 100개, 1000개가 되면 어떻게 관리하지?"
   → 서버 1번에 컨테이너 배치... 서버 2번에 배치... 
   → 서버 3번 죽었다! 어떡하지?
   → 트래픽 폭주! 컨테이너 더 필요해!
   → 사람이 수동으로? 불가능! 😱

✅ 해결: 쿠버네티스(K8s) 등장!
   → 컨테이너 자동 배치, 자동 스케일링, 자동 복구
   → Google이 15년간 사용한 Borg 시스템 기반
   → 컨테이너계의 "자동 관리 로봇" 🤖
```

---

## 📝 개념 정의

> **쿠버네티스(Kubernetes, K8s)**란?
> 
> 🐳 **컨테이너화된 애플리케이션**을  
> 🎛️ **자동으로 배포, 스케일링, 관리**하는  
> 🌐 **오픈소스 오케스트레이션 플랫폼**

### 쉬운 비유

| 오케스트라 | 쿠버네티스 |
|:---------|:----------|
| 🎻 악기 연주자들 | 컨테이너(Pod)들 |
| 🎼 악보 | 설정 파일(YAML) |
| 👨‍🎤 지휘자 | 쿠버네티스 |
| "바이올린 소리 커!" | "Pod 3개 더 만들어!" |

---

## 🏗️ 기술요소 (2가지 그룹)

### 그룹 1: Master Node (지휘본부)

| 컴포넌트 | 역할 | 쉬운 설명 |
|:--------|:----|:---------|
| **API Server** | 모든 명령의 입구 | 📞 안내데스크 |
| **Scheduler** | Pod를 어느 서버에 배치할지 결정 | 📋 배치 담당자 |
| **Controller Manager** | 원하는 상태 유지 | 🔧 유지보수 담당 |
| **etcd** | 모든 정보 저장소 | 📁 데이터베이스 |

### 그룹 2: Worker Node (실제 일하는 곳)

| 컴포넌트 | 역할 | 쉬운 설명 |
|:--------|:----|:---------|
| **Pod** | 컨테이너가 실행되는 최소 단위 | 📦 컨테이너 박스 |
| **Kubelet** | Master와 통신하는 에이전트 | 📡 현장 관리자 |
| **Kube-proxy** | 네트워크 트래픽 관리 | 🚦 교통 정리 |
| **Container Runtime** | 실제 컨테이너 실행 | 🏃 Docker/containerd |

---

## ⭐ 차별점 키워드 (답안지 가산점!)

{: .highlight }
> **HPA (Horizontal Pod Autoscaler)**
> 
> Pod의 CPU/메모리 사용량을 모니터링해서 **자동으로 Pod 수를 조절**
> 
> - CPU 80% 넘으면 → Pod 2개 → 4개로 Scale Out
> - CPU 20% 이하면 → Pod 4개 → 2개로 Scale In
> - 트래픽 폭주에 자동 대응! 💪

---

## 🔗 쿠버네티스 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    Kubernetes Cluster                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Control Plane (Master)                  │   │
│  │  ┌──────────┐ ┌───────────┐ ┌──────────────────┐   │   │
│  │  │API Server│ │ Scheduler │ │Controller Manager│   │   │
│  │  └────┬─────┘ └───────────┘ └──────────────────┘   │   │
│  │       │                                             │   │
│  │  ┌────┴────┐                                        │   │
│  │  │  etcd   │ ← 모든 클러스터 데이터 저장             │   │
│  │  └─────────┘                                        │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                 │
│           ┌───────────────┼───────────────┐                │
│           │               │               │                │
│           ▼               ▼               ▼                │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │
│  │ Worker Node │ │ Worker Node │ │ Worker Node │          │
│  │ ┌─────────┐ │ │ ┌─────────┐ │ │ ┌─────────┐ │          │
│  │ │ kubelet │ │ │ │ kubelet │ │ │ │ kubelet │ │          │
│  │ ├─────────┤ │ │ ├─────────┤ │ │ ├─────────┤ │          │
│  │ │  proxy  │ │ │ │  proxy  │ │ │ │  proxy  │ │          │
│  │ ├─────────┤ │ │ ├─────────┤ │ │ ├─────────┤ │          │
│  │ │Pod │Pod │ │ │ │Pod │Pod │ │ │ │Pod │Pod │ │          │
│  │ └─────────┘ │ │ └─────────┘ │ │ └─────────┘ │          │
│  └─────────────┘ └─────────────┘ └─────────────┘          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 6하원칙 요약

| 구분 | 내용 |
|:-----|:----|
| **누가(Who)** | Google → CNCF(Cloud Native Computing Foundation) |
| **언제(When)** | 2014년 오픈소스 공개, 현재 클라우드 표준 |
| **어디서(Where)** | 클라우드(AWS EKS, GKE, AKS), 온프레미스 |
| **무엇을(What)** | 컨테이너화된 마이크로서비스 애플리케이션 |
| **왜(Why)** | 대규모 컨테이너 자동 관리, 무중단 배포 |
| **어떻게(How)** | 선언적 설정(YAML) + 자동 스케일링 + 자가 치유 |

---

# 🎯 기술사 수준 설명
{: .fs-7 }

> 정보관리기술사 수준으로 설명합니다

---

## 🔑 핵심 키워드 3개

| 키워드 | 기술 의미 |
|:------|:---------|
| **Container Orchestration** | 컨테이너 배포/스케일링/네트워킹 자동화 플랫폼 |
| **Desired State Management** | 선언적 설정 기반 상태 수렴(Reconciliation) |
| **Cloud Native** | MSA, CI/CD, DevOps 생태계의 핵심 인프라 |

---

## 📖 등장배경

| 배경 | 설명 |
|:----|:-----|
| **컨테이너 확산** | Docker 대중화로 대규모 컨테이너 관리 필요성 증가 |
| **MSA 전환** | 마이크로서비스 아키텍처에서 수백 개 서비스 운영 필요 |

---

## 📝 개념 정의 (22자 이내)

> **컨테이너 자동배포·스케일링 오케스트레이션 플랫폼**

---

## 🏗️ 구성요소 (2그룹 × 4항목)

### 그룹 1: Control Plane (Master Node)

| 구성요소 | 영문 | 역할 | 특징 |
|:--------|:----|:----|:-----|
| **API Server** | kube-apiserver | REST API 엔드포인트, 모든 요청의 진입점 | kubectl, helm 명령 수신 |
| **Scheduler** | kube-scheduler | Pod를 적절한 Node에 할당 | 리소스/affinity 기반 배치 |
| **Controller Manager** | kube-controller-manager | 상태 관리 컨트롤러 집합 | ReplicaSet, Node, Endpoint 컨트롤러 |
| **etcd** | etcd | 분산 key-value 저장소 | 클러스터 상태/설정 영속화 |

### 그룹 2: Worker Node (Data Plane)

| 구성요소 | 영문 | 역할 | 특징 |
|:--------|:----|:----|:-----|
| **Pod** | Pod | 컨테이너 실행 최소 단위 | 1개 이상의 컨테이너 포함 |
| **Kubelet** | kubelet | Node 에이전트 | API Server와 통신, Pod 라이프사이클 관리 |
| **Kube-proxy** | kube-proxy | 네트워크 프록시 | Service → Pod 트래픽 라우팅 |
| **Container Runtime** | CRI | 컨테이너 실행 엔진 | containerd, CRI-O, Docker |

---

## 🧠 암기법 (Mnemonic)

{: .note }
> ### 그룹 1 (Master): **"A-S-C-E"** (API → Scheduler → Controller → Etcd)
> 
> | 암기 | 컴포넌트 | 역할 |
> |:-----|:--------|:-----|
> | **A** | **A**PI Server | 모든 요청의 입구 |
> | **S** | **S**cheduler | Pod 배치 결정 |
> | **C** | **C**ontroller Manager | 상태 유지 관리 |
> | **E** | **E**tcd | 데이터 저장소 |
>
> 💡 **"ASCE"** — API로 받아서, Schedule하고, Control해서, Etcd에 저장

{: .note }
> ### 그룹 2 (Worker): **"P-K-K-C"** (Pod → Kubelet → Kube-proxy → Container)
> 
> | 암기 | 컴포넌트 | 역할 |
> |:-----|:--------|:-----|
> | **P** | **P**od | 컨테이너 실행 단위 |
> | **K** | **K**ubelet | Node 에이전트 |
> | **K** | **K**ube-proxy | 네트워크 프록시 |
> | **C** | **C**ontainer Runtime | 컨테이너 엔진 |
>
> 💡 **"PKKC"** — Pod를 Kubelet이 관리, Kube-proxy가 네트워크, Container Runtime이 실행

---

## 📊 쿠버네티스 특징

| 특징 | 설명 |
|:----|:-----|
| **클러스터 기반** | 수백~수천 대 서버를 마스터 노드를 통해 중앙 관리 |
| **상태 관리** | Desired State 선언 → 자동으로 Current State 수렴 |
| **배포 관리** | Rolling Update, Blue-Green, Canary 배포 지원 |
| **서비스 디스커버리** | DNS 기반 서비스 등록/조회, 로드밸런싱 |
| **자가 치유** | Pod/Node 장애 시 자동 재시작/재배치 |

---

## ⭐ HPA (Horizontal Pod Autoscaler)

{: .important }
> ### HPA 개념
> 
> 애플리케이션 **부하(CPU/메모리)에 따라 Pod 수를 자동 조절**하는 오토스케일링 기능
>
> ### HPA vs VPA 비교
>
> | 구분 | HPA | VPA |
> |:----|:----|:----|
> | **방향** | Horizontal (수평) | Vertical (수직) |
> | **스케일링** | Pod 개수 증감 | Pod 리소스(CPU/Mem) 증감 |
> | **적용** | replicas 조정 | requests/limits 조정 |
> | **재시작** | 불필요 | Pod 재시작 필요 |
> | **활용** | Stateless 워크로드 | Stateful 워크로드 |

### HPA 동작 원리

```
┌─────────────────────────────────────────────────────────────┐
│                    HPA 동작 흐름                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ┌─────────────┐    메트릭 수집     ┌─────────────────┐    │
│   │   Metrics   │ ◄───────────────  │   Pod 1,2,3     │    │
│   │   Server    │   (CPU, Memory)   │   (현재 3개)     │    │
│   └──────┬──────┘                   └─────────────────┘    │
│          │                                   ▲              │
│          │ 메트릭 조회                        │ replicas    │
│          ▼                                   │ 조정        │
│   ┌─────────────┐    replicas 변경   ┌──────┴────────┐    │
│   │     HPA     │ ─────────────────► │  Deployment   │    │
│   │ Controller  │                    │  Controller   │    │
│   └─────────────┘                    └───────────────┘    │
│                                                             │
│   [계산식]                                                  │
│   desiredReplicas = ceil(currentReplicas ×                 │
│                          (currentMetric / desiredMetric))  │
│                                                             │
│   예: CPU 목표 50%, 현재 80%, 현재 3개                       │
│       → ceil(3 × 80/50) = ceil(4.8) = 5개로 Scale Out      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔗 쿠버네티스 오브젝트 계층

```
┌─────────────────────────────────────────────────────────────┐
│                  Kubernetes Object Hierarchy                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   Namespace                                                 │
│       │                                                     │
│       ├── Deployment / StatefulSet / DaemonSet              │
│       │       │                                             │
│       │       └── ReplicaSet                                │
│       │               │                                     │
│       │               └── Pod                               │
│       │                     │                               │
│       │                     └── Container                   │
│       │                                                     │
│       ├── Service (ClusterIP, NodePort, LoadBalancer)       │
│       │                                                     │
│       ├── ConfigMap / Secret                                │
│       │                                                     │
│       ├── PersistentVolumeClaim                             │
│       │                                                     │
│       └── HPA / VPA                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 6하원칙 요약

| 구분 | 내용 |
|:----|:-----|
| **누가(Who)** | Google(Borg 기반) → CNCF 주관 오픈소스 |
| **언제(When)** | 2014 공개 → 2015 v1.0 → 현재 Cloud Native 표준 |
| **어디서(Where)** | 퍼블릭 클라우드(EKS, GKE, AKS), 온프레미스, 하이브리드 |
| **무엇을(What)** | 컨테이너화된 MSA 워크로드, CI/CD 파이프라인 |
| **왜(Why)** | 대규모 컨테이너 관리, 무중단 배포, 리소스 효율화 |
| **어떻게(How)** | 선언적 YAML + Controller Loop + Reconciliation |

---

## 📈 상위 토픽 계층도

```
최상위: IT 서비스
    │
    ├── 클라우드 컴퓨팅 (Cloud Computing)
    │       │
    │       ├── 클라우드 네이티브 (Cloud Native)
    │       │       │
    │       │       ├── 컨테이너 기술
    │       │       │       ├── Docker
    │       │       │       └── containerd
    │       │       │
    │       │       ├── 🎯 컨테이너 오케스트레이션
    │       │       │       └── Kubernetes (K8s)
    │       │       │               ├── Control Plane
    │       │       │               ├── Worker Node
    │       │       │               └── HPA/VPA
    │       │       │
    │       │       └── 서비스 메시 (Istio, Linkerd)
    │       │
    │       └── 관리형 쿠버네티스
    │               ├── AWS EKS
    │               ├── Google GKE
    │               └── Azure AKS
    │
    └── DevOps / SRE
            ├── CI/CD (Jenkins, ArgoCD)
            ├── GitOps
            └── Infrastructure as Code
```

---

## 🔮 향후 전망

### 기술적 전망

| 전망 | 설명 |
|:----|:-----|
| **서버리스 통합** | Knative, KEDA 기반 이벤트 드리븐 스케일링 |
| **멀티클러스터** | Federation, Cluster API로 클러스터 간 워크로드 분산 |
| **eBPF 활용** | Cilium 기반 고성능 네트워킹/보안 |

### 서비스/산업 전망

| 전망 | 설명 |
|:----|:-----|
| **엣지 컴퓨팅** | K3s, MicroK8s로 엣지 환경 확장 |
| **AI/ML 워크로드** | Kubeflow, GPU 스케줄링 최적화 |
| **관리형 서비스 확대** | EKS Anywhere, GKE Autopilot 등 |

### 보안/운영 전망

| 전망 | 설명 |
|:----|:-----|
| **Supply Chain 보안** | SBOM, Sigstore 기반 이미지 서명 |
| **Policy as Code** | OPA/Gatekeeper 기반 정책 관리 |
| **GitOps 표준화** | ArgoCD, Flux 기반 선언적 배포 |

---

## 📚 참고자료

- ITPE 기술사회 자료집
- Kubernetes 공식 문서 (kubernetes.io)
- CNCF Landscape
- "Kubernetes in Action" (Marko Lukša)

---

## ✅ 학습 체크리스트

- [ ] 쿠버네티스 개념 및 특징 이해
- [ ] Control Plane 4대 컴포넌트 암기 (A-S-C-E)
- [ ] Worker Node 4대 컴포넌트 암기 (P-K-K-C)
- [ ] Pod, Deployment, Service 관계 이해
- [ ] HPA vs VPA 차이점 파악
- [ ] HPA 스케일링 계산식 이해

---

*Last Updated: 2025-12-02*

