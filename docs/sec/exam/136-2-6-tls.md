---
layout: default
title: 136회-2교시-6번 TLS 프로토콜
parent: 📝 기출문제
grand_parent: SEC (정보보안)
nav_order: 136206
permalink: /docs/sec/exam/136-2-6-tls
---

# TLS (Transport Layer Security)
{: .no_toc }

136회 정보관리기술사 2교시 6번
{: .label .label-blue }

보안 / 난이도: 상
{: .label .label-yellow }

2교시형 (서술형)
{: .label .label-red }

---

## 📚 목차
{: .no_toc .text-delta }

- [🎓 고딩 수준 설명](#-고딩-수준-설명)
- [🎯 기술사 수준 설명](#-기술사-수준-설명)

---

<details markdown="1">
<summary><h2 style="display:inline" id="-고딩-수준-설명">🎓 고딩 수준 설명 (클릭해서 펼치기)</h2></summary>

### 🔑 핵심 키워드 3개
- **암호화 통신**: 데이터를 암호화해서 안전하게 전송
- **핸드셰이크**: 통신 전 서로 인사하고 약속하는 과정
- **HTTPS**: HTTP + TLS = 안전한 웹 통신

### 📖 등장배경

> 인터넷에서 비밀번호나 카드 정보를 그냥 보내면 해커가 훔쳐볼 수 있어요. 그래서 **데이터를 암호화**해서 보내는 TLS가 만들어졌어요. TLS 1.2에 취약점이 발견되어 더 안전한 **TLS 1.3**을 사용하도록 권고하고 있어요!

### 📝 개념 정의

> TLS는 **"인터넷 통신에서 데이터를 암호화해서 안전하게 주고받는 보안 프로토콜"**이에요.
> 
> 비유하자면, 편지를 **비밀 상자**에 넣어서 보내고, 받는 사람만 열 수 있는 **열쇠**를 주는 것과 같아요!

### 🏗️ TLS 1.2 vs 1.3 비교

| 구분 | TLS 1.2 | TLS 1.3 |
|:-----|:--------|:--------|
| 속도 | 느림 (2-RTT) | 빠름 (1-RTT) |
| 암호화 | 일부 취약 알고리즘 허용 | 강력한 알고리즘만 |
| 핸드셰이크 | 5~7 패킷 | 0~3 패킷 |
| 보안 | 취약점 존재 | 대폭 개선 |

### ⭐ 차별점 키워드

> **0-RTT**: TLS 1.3에서 재접속 시 바로 데이터 전송 가능!

</details>

---

## 🎯 기술사 수준 설명
{: #-기술사-수준-설명}

### 📌 핵심 암기 (Quick Reference)

{: .highlight }
> **TLS**: 기밀성, 무결성, 인증
> - (프로토콜) `RHCA` (Record, Handshake, ChangeCipher, Alert)
> - (핸드셰이크) `CS인CF` (ClientHello, ServerHello, 인증키교환, ChangeCipherSpec, Finished)
> - (1.3 개선) `취간단0` (취약알고리즘제거, 간소화, 단순화, 0-RTT)

---

## Ⅰ. SSL의 후속 표준, TLS(Transport Layer Security)의 개념

### 가. TLS의 정의

인터넷 통신 보안을 위한 **TLS 프로토콜의 이해** 필요 (SSL 3.0 이후 버전: TLS 1.0 → 1.1 → 1.2 → 1.3)

```
┌─────────────────────────────────────────────────────────────────────┐
│          HTTP                    HTTP          │   응용 계층        │
│                               ┌─────────┐      │                   │
│           ↓                   │TLS or SSL│      │   보안 계층        │
│                               └─────────┘      │                   │
│          TCP                    TCP            │   전송 계층        │
│           ↓                      ↓             │                   │
│          IP                     IP             │   네트워크 계층     │
│           ↓                      ↓             │                   │
│    Network Interface     Network Interface     │   데이터링크 계층   │
├─────────────────────────────────────────────────────────────────────┤
│         HTTP                  HTTPS                                │
└─────────────────────────────────────────────────────────────────────┘
```

| 구분 | 내용 |
|:-----|:-----|
| **정의** | 네트워크를 통해 데이터를 송수신할 때 **기밀성(confidentiality), 무결성(integrity), 인증(authenticity)**을 제공하기 위해 설계된 암호화 통신 프로토콜 |

| 특징 | 설명 |
|:----|:-----|
| **기밀성** | 제3자가 내용을 볼 수 없도록 대칭키 기반 암호화 수행 |
| **무결성** | 메시지가 위조되거나 변조되지 않았음을 MAC 등으로 확인 |
| **인증** | 디지털 인증서를 통해 서버 또는 양측 신원 확인 가능 |
| **키 협상** | 안전한 세션 키 생성을 위해 비대칭키 기반 키 교환 수행 |
| **애플리케이션 독립성** | HTTPS, SMTPS, FTPS 등 다양한 프로토콜과 결합 가능 |

> TLS 구조는 효율성, 확장성, 보안성 등을 목표로 설계되어 다양한 환경에서 안전한 통신을 보장함

### 나. TLS의 프로토콜 구조 `RHCA`

```
+--------------------------------+
|     Application Data           | ← 사용자 데이터 (예: HTTP)
+--------------------------------+
|     Record Protocol            | ← 암호화, 압축, MAC 처리
+--------------------------------+
|   Handshake Protocol           | ← 인증, 키 교환, 암호 협상
|   ChangeCipherSpec             | ← 암호 모드 전환 알림
|   Alert Protocol               | ← 경고 및 오류 알림
+--------------------------------+
|   Transport Layer (TCP)        | ← 신뢰성 있는 전송 (기반 계층)
+--------------------------------+
```

| 프로토콜 | 주요 역할 및 기능 |
|:--------|:----------------|
| **Record Protocol** | 데이터 단편화, 압축, 암호화, 전송 |
| **Handshake Protocol** | 암호화 알고리즘 협상, 인증서 교환, 키 교환 등 세션 설정 |
| **Change Cipher Spec Protocol** | 암호 스펙 변경 (세션 키 설정 완료시점 알림) |
| **Alert Protocol** | 에러, 경고 등 통신 중 발생하는 문제 알림 |

> TLS는 인터넷 환경에서 안전한 데이터 전송을 가능하게 하는 국제 표준 보안 프로토콜로, 암호화, 무결성 검증, 인증을 통해 **신뢰성 있는 통신 채널을 보장**

---

## Ⅱ. TLS 프로토콜 핸드셰이크(Handshake) 과정

### 가. TLS 프로토콜 핸드셰이크 과정 비교

```
       TLS 1.2 Handshake                    TLS 1.3 Handshake
       
  Client          Server              Client          Server
    │                │                  │                │
    │  ClientHello   │                  │  ClientHello   │
    │───────────────>│                  │───────────────>│
    │                │                  │ (키 공유 포함)   │
    │   ServerHello  │                  │                │
    │   Certificate  │                  │  ServerHello   │
    │  ServerHelloDone│                 │  + Finished    │
    │<───────────────│                  │<───────────────│
    │                │                  │(모든 이후 암호화됨)│
    │ClientKeyExchange│                 │                │
    │ChangeCipherSpec│                  │   Finished     │
    │   Finished     │                  │───────────────>│
    │───────────────>│                  │                │
    │                │                  │                │
    │ChangeCipherSpec│
    │   Finished     │
    │<───────────────│
```

> TLS 1.3은 ClientHello 단계에서 키 교환을 포함시켜 서버가 즉시 응답(왕복 횟수 감소), 모든 메시지 암호화

### 나. TLS 프로토콜 핸드셰이크 상세 설명

**TLS 1.2 핸드셰이크** `CS인CF`

| 단계 | 핸드셰이크 과정 | 상세 설명 |
|:-----|:-------------|:---------|
| **C**lientHello | 핸드셰이크 개시 | 클라이언트가 서버에 지원하는 TLS 버전, 암호 스위트 목록, 랜덤 값 등을 보냄 |
| **S**erverHello | 서버 인증서 및 키 교환 메시지 | 서버가 선택한 암호 스위트와 함께 디지털 인증서, 서버 랜덤 값, 키 교환 정보(ECDHE 또는 RSA 등)를 전달 |
| **인**증 및 키 교환 | 클라이언트 키 교환 | 클라이언트가 pre-master secret을 서버의 공개키로 암호화하여 전송 |
| **C**hangeCipherSpec | 암호 모드 전환 알림 | 클라이언트가 암호화 모드로 전환되었음을 서버에 알림 |
| **F**inished | 무결성 확인 메시지 | 클라이언트가 생성한 세션 키로 MAC을 포함한 Finished 메시지를 전송 |

**TLS 1.3 핸드셰이크**

| 단계 | 핸드셰이크 과정 | 상세 설명 |
|:-----|:-------------|:---------|
| ClientHello | 키 공유 포함 | 클라이언트가 암호 스위트, 랜덤값, 공개키(ECDHE), 지원 버전 등을 보냄 |
| ServerHello + Finished | 즉시 응답 | 서버가 바로 ServerHello를 응답하고 동시에 Finished 메시지를 보냄 (여기부터 암호화 시작됨) |
| Finished | 클라이언트 검증 | 클라이언트가 서버의 Finished 메시지를 검증하고, 자체 Finished 메시지를 암호화하여 응답 |
| (옵션) 0-RTT | 재접속 최적화 | 클라이언트가 이전 세션 정보로 즉시 애플리케이션 데이터를 보낼 수 있음 (재생 공격 위험 있음) |

> TLS 1.2 왕복 횟수(RTT) 최소 **2-RTT** 필요, TLS 1.3 기본 **1-RTT**, **0-RTT** 지원 (재연결 시)

---

## Ⅲ. TLS 1.2 보안 취약점과 TLS 1.3 개선 사항

### 가. TLS 1.2 보안 취약점

| 구분 | 취약점 | 상세 설명 |
|:-----|:------|:---------|
| **암호 알고리즘** | SHA-1, MD5 등의 취약 알고리즘 허용 | TLS 1.2는 안전하지 않은 해시 알고리즘(SHA-1, MD5 등) 사용 가능 → 충돌 공격에 취약 |
| | Forward Secrecy 미지원 가능 | RSA 기반 키 교환 시 개인키 유출 시 과거 통신 복호화 위험 |
| **프로토콜 구조** | 다운그레이드 공격 가능 | ClientHello 조작을 통해 약한 암호 스위트 협상 유도 가능 (예: POODLE, BEAST 등) |
| | 핸드셰이크 평문 구간 존재 | ServerHello 이전까지는 메시지가 암호화되지 않아 MITM(중간자 공격)에 노출 가능 |
| **구현 복잡성 및 성능** | 구현 오류 가능성 | 암호 스위트·키 교환 방식 다양 → 구현 복잡도 증가, 취약점 발생 가능성 상승 |
| | 핸드셰이크 다단계로 인한 지연 | 2-RTT 이상 절차로 초기 연결 시간 지연 발생, 모바일 환경 등에서 비효율적 |

> 취약한 암호화 알고리즘 허용으로 인해 보안 취약점이 존재하여, **TLS 1.3으로 개선**

### 나. TLS 1.3 개선 사항 `취간단0`

| 구분 | 개선 사항 | 상세 설명 |
|:-----|:---------|:---------|
| **암호 알고리즘** | **취**약 알고리즘 완전 제거, SHA-2 계열만 허용 | 안전하지 않은 해시 함수 제거로 충돌 공격 차단, 인증서 서명도 SHA-256 이상만 허용됨 |
| | ECDHE 방식만 허용, Forward Secrecy 강제 적용 | 세션키 유출 시에도 과거 통신 복호화 방지 |
| **프로토콜 구조** | 버전 협상 구조 **간**소화, downgrade 방지 필드 삽입 | 의도적 다운그레이드 방지 및 취약한 TLS/SSL 버전 차단 설정 |
| | ServerHello 이후 모든 메시지 암호화 | TLS 1.3은 핸드셰이크 대부분을 암호화함. 민감 데이터는 평문 교환 구간에서 배제 |
| **구현 복잡성 및 성능** | 암호 스위트 **단**순화 및 구조 단순화 | 복잡성 감소로 인한 구현 취약점 줄어듦 |
| | 1-RTT 핸드셰이크, **0**-RTT 지원 (재연결 시) | 절차를 줄여 초기 연결 속도 향상, 사용자 경험 개선 (단, 0-RTT는 일부 보안 주의 필요) |

---

## Ⅳ. TLS 1.2 버전과 TLS 1.3 버전 특징 비교

| 구분 | TLS 1.2 | TLS 1.3 |
|:-----|:--------|:--------|
| **암호화 알고리즘** | 취약 알고리즘 지원 (SHA-1, RC4 등) | 강력한 알고리즘만 지원, 취약 알고리즘 완전 제거 |
| **핸드셰이크** | 복잡, 일부 평문 노출 | 과정 단순화, 패킷 수 감소 |
| **암호 제품군** | 다양, 취약 조합 가능 | 단순화, 안전한 조합만 허용 |
| **성능** | 핸드셰이크 지연 (5~7 패킷) | 핸드셰이크 속도 향상 (0~3 패킷), 0-RTT 지원 |
| **재협상** | 지원 (공격 벡터 존재) | 제거 |
| **Forward Secrecy** | 선택적 | 필수 (ECDHE만 허용) |

> TLS 1.3은 구조적, 암호학적, 성능적 측면에서 기존 버전의 한계를 극복하고, 안전한 전송계층 보안 프로토콜로 **HTTP/3** 등에서도 활용되고 있음

---

### 📈 상위 토픽 계층도

```
네트워크 보안 프로토콜
├── SSL (Secure Sockets Layer)
│   └── SSL 3.0
└── TLS (Transport Layer Security) ← 현재 토픽
    ├── TLS 1.0, 1.1 (deprecated)
    ├── TLS 1.2 (취약점 존재)
    └── TLS 1.3 (권고)
        ├── 0-RTT
        ├── ECDHE 필수
        └── HTTP/3 적용
```

---

### ✅ 학습 체크리스트

- [ ] TLS의 정의와 특징(기밀성, 무결성, 인증)을 설명할 수 있다
- [ ] TLS 프로토콜 구조(RHCA)를 나열할 수 있다
- [ ] TLS 1.2 핸드셰이크 과정(CS인CF)을 설명할 수 있다
- [ ] TLS 1.2와 1.3의 핸드셰이크 차이(RTT)를 비교할 수 있다
- [ ] TLS 1.2의 취약점(암호 알고리즘, 프로토콜 구조, 복잡성)을 알고 있다
- [ ] TLS 1.3의 개선 사항(취간단0)을 설명할 수 있다

